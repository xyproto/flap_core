// reduce folds a list with a binary function and initial value using tail recursion
// reduce(acc, x => acc + x, [1, 2, 3, 4], 0) -> 10
reduce = f, list, init => (#list == 0) { -> init ~> me(f, list[1::], f(init, list[0])) }
